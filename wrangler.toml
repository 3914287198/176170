name = "176170-xyz-comments"
main = "worker.js"
compatibility_date = "2024-01-01"

# D1 数据库绑定
[[d1_databases]]
binding = "DB"
database_name = "176170_xyz"
database_id = "a5ca1d3c-844a-4bac-9a81-b77ee9b39fad"

# KV 绑定 (可选，如果需要缓存)
# [[kv_namespaces]]
# binding = "CACHE"
# id = "your-kv-namespace-id"

# 环境变量（钉钉机器人配置）
[vars]
# 注意：为了安全起见，建议使用 wrangler secret 命令设置敏感信息
# 例如：wrangler secret put DINGTALK_ACCESS_TOKEN
#       wrangler secret put DINGTALK_SECRET
# 也可以直接在这里配置（但不推荐在生产环境中这样做）
# DINGTALK_ACCESS_TOKEN = "your_access_token"
# DINGTALK_SECRET = "your_secret"
